# Generated assets

`untitled-icons.ts` is written by `npm run gen:untitled-icons` (also `predev` / `prebuild` / `postinstall`) from the pinned `@untitledui/icons` package.

It is gitignored: Untitled UI's license allows using the icons in a product, not redistributing the glyph set. The npm package is the source of truth.
