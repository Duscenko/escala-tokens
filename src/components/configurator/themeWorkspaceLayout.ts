/** Shared outer boundary for every drawer opened from the Themes workspace. */
export const THEME_LIBRARY_WIDTH = 196

/** Subtle glass fills for workspace chips — rest / hover / active tiers, no border. */
export const WORKSPACE_CHIP_REST = 'bg-white/35 dark:bg-white/[0.04]'
export const WORKSPACE_CHIP_HOVER = 'hover:bg-white/50 dark:hover:bg-white/[0.07]'
export const WORKSPACE_CHIP_ACTIVE = 'bg-white/55 dark:bg-white/[0.08]'

/** Gray track behind `ThemeViewSwitcher` — shared by TopNav session controls. */
export const CHROME_CONTROL_SHELL = 'border border-line bg-elevated/70 shadow-sm'
export const CHROME_CONTROL_HOVER = 'hover:border-line-strong hover:bg-surface'
export const CHROME_CONTROL_ACTIVE = 'border-line-strong bg-surface'
export const CHROME_CONTROL_FOCUS = 'focus-within:border-line-strong focus-within:bg-surface focus-within:text-fg'
