/** Shared outer boundary for every drawer opened from the Themes workspace. */
export const THEME_LIBRARY_WIDTH = 196

/** Subtle glass fills for workspace chips — rest / hover / active tiers, no border. */
export const WORKSPACE_CHIP_REST = 'bg-white/35 dark:bg-white/[0.04]'
export const WORKSPACE_CHIP_HOVER = 'hover:bg-white/50 dark:hover:bg-white/[0.07]'
export const WORKSPACE_CHIP_ACTIVE = 'bg-white/55 dark:bg-white/[0.08]'
